CREATE TABLE `jobs` (
  `id` INT,
  `position` TEXT,
  `company` TEXT,
  `companyLogo` TEXT,
  `location` TEXT,
  `date` DATE,
  `agoTime` TEXT,
  `salary` TEXT,
  `jobUrl` TEXT,
  `source` TEXT,
  `description` TEXT,
  `easy_apply` TINYINT(1),
  `applicationUrl` TEXT,
  `applicationStatus` TEXT,
  `work_type` TEXT,
  `sector` TEXT,
  `updateDate` DATE
);
